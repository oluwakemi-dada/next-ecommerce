import CartTable from './cart-table';

export const metadata = {
  title: 'Shopping Cart',
};

const CartPage = () => {
  return (
    <>
      <CartTable />
    </>
  );
};

export default CartPage;
